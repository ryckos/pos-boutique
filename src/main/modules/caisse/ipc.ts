/**
 * Propriétaire : Dev A.
 * Point d'enregistrement des canaux caisse:* (sessions, ventes, paiements, tickets).
 * Organisation conseillée du module :
 *   service-session.ts · service-vente.ts · calculs.ts (TVA, totaux — testés) · ipc.ts
 * Voir docs/DEV_A_COMPTOIR.md, tâches A1 à A5.
 */
export function enregistrerIpcCaisse(): void {
  // Phase 1 : gerer('caisse:enregistrerVente', …) etc.
}
